#include <stdio.h>
#include <math.h>

/// Typedef
typedef long long ll;

#define sci1(a) scanf("%d", &a)
#define sci2(a, b) scanf("%d %d", &a, &b)

#define scll1(a) scanf("%lld", &a)
#define scll2(a, b) scanf("%lld %lld", &a, &b)

#define scd1(a) scanf("%lf", &a)
#define scd2(a, b) scanf("%lf %lf", &a, &b)

#define pfi1(a) printf("%d\n", a)
#define pfi2(a, b) printf("%d %d\n", a, b)

#define pfll1(a) printf("%lld\n", a)
#define pfll2(a, b) printf("%lld %lld\n", a, b)

#define pfd1(a) printf("%lf\n", a)
#define pfd2(a, b) printf("%lf %lf\n", a, b)

#define fget(str) fgets(str, sizeof(str), stdin)

#define r return

int main()
{
    //freopen("input.txt", "r", stdin);
    //freopen("output.txt", "w", stdout);
    int test, n;

    scanf("%d", &test);

    while (test--)
    {
        scanf("%d", &n);
    }

    r 0;
}